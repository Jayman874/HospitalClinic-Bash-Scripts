# HospitalClinic-Bash-Scripts

Bash Scripts which creates directories for a Hospital Clinic call Wellington Clinic

It creates all the directories and sets up user permissions and directory permissions
